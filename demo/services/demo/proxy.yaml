proxy:
  owner: demo
  # Imitates upstream services/is/ps7.yaml egress-proxies + services/is/squid.yaml
  hosts:
    internal:
      - hosts/demo-proxy   # squid-proxy LXD VM (10.142.65.2)
                           # Upstream equivalent: ps7-egress-proxy (91.189.92.10/11/12)
